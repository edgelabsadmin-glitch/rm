# SPEC-001 / ADR-002 — pulse-flows (Activepieces self-hosted).
# Thin wrapper over the official image so the deploy is pin-able and Fly can
# build from this directory. Pin to a dated tag before production (replace
# :latest with the version you validated).
FROM activepieces/activepieces:latest
