# SPEC-001 / ADR-003 — pulse-langfuse (Langfuse self-hosted, LLM observability).
# Langfuse v2: single container, Postgres-only (no Clickhouse/Redis/S3) — the
# lean ADR-003 Phase-1 choice. v3 self-host adds heavy infra; revisit later.
# Pin to a dated v2 tag before production.
FROM langfuse/langfuse:2
